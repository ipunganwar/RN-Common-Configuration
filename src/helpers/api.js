const development = {
  	module: `http://`,
  }
const staging = {
  	module: `http://`,
  }
const production = {
  	module: `http://`,
	}
const donkeyCamp = {
	module: 'http://',
}

const apiKey1 = 'api_key1'
const apiKey2 = 'api_key2'
const apiKey3 =  'api_key3'

let connection = {
	server: development,
	apiKey: apiKey1
}
export { connection }

